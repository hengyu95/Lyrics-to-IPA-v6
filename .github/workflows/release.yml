name: Build and Release

on:
  push:
    tags:
      - 'v*' 

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        os: [windows-latest, macos-latest]

    steps:
    - name: Check out code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller PyQt5 eng_to_ipa svgwrite

    - name: Build on Windows
      if: matrix.os == 'windows-latest'
      run: pyinstaller --onefile --windowed --collect-data eng_to_ipa --collect-submodules PyQt5.QtMultimedia --add-data "Audio;Audio" --hidden-import svgwrite LyricsToIPA.py

    - name: Build on Mac
      if: matrix.os == 'macos-latest'
      # Notice the colon (:) in Audio:Audio
      run: pyinstaller --onefile --windowed --collect-data eng_to_ipa --collect-submodules PyQt5.QtMultimedia --add-data "Audio:Audio" --hidden-import svgwrite LyricsToIPA.py

    - name: Zip Mac App
      if: matrix.os == 'macos-latest'
      run: |
        cd dist
        zip -r LyricsToIPA-Mac.zip LyricsToIPA.app

    - name: Upload to GitHub Releases
      uses: softprops/action-gh-release@v1
      with:
        files: |
          dist/*.exe
          dist/LyricsToIPA
          dist/*.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}